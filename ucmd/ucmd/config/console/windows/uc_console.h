#ifndef UC_CONSOLE_H
#define UC_CONSOLE_H

/* This is the root configuration file
   used when compiling on Windows machines. */
   
/* We include another file that defines the
   declspec import or export settings. */
#include "uc_export.h"

#endif
